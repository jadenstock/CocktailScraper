[api]
brave_api_key=""
openai_api_key=""

[search]
results_per_query = 4
bars_per_city = 4

[model]
name = "gpt-3.5-turbo"

[tracking]
log_file = "usage_logs.jsonl"
max_cost_per_session = 0.50  # Optional: add budget limit